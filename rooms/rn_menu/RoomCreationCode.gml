
/*var layer_id = layer_get_id("Background"); // Replace Background with the name of the background layer you want to change.
var spd = choose(-2,-1,1,2);
layer_hspeed(layer_id, spd);
layer_vspeed(layer_id, spd);
*/

audio_play_sound(snd_fundo,1,true);
